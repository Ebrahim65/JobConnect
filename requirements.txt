# Core FastAPI & ASGI
fastapi==0.109.2
uvicorn==0.27.0
httptools==0.6.1  # Optional but recommended for UVicorn performance

# Pydantic & Settings
pydantic==2.6.4
pydantic-settings==2.2.1

# Database (PostgreSQL + Async)
asyncpg==0.29.0
sqlalchemy==2.0.25
geoalchemy2==0.15.0

# Security & Auth
python-jose==3.3.0
passlib==1.7.4
cryptography==42.0.5  # Required for JOSE

# Utilities
requests==2.31.0
googlemaps==4.10.0
jinja2==3.1.3  # For HTML templating (PDF generation)
xhtml2pdf==0.2.11  # PDF generation

# CORS (if your frontend connects to this API)
starlette==0.36.3  # FastAPI depends on it, but pinning for safety

# Optional (uncomment if used)
# python-multipart==0.0.6  # Only needed for file uploads
# aiofiles==23.2.1  # For async file handling